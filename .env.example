# Optional AI form parser and existing evidence helper. Never commit real keys.
# The application reads OPENAI_API_KEY from the local process environment.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini-2025-04-14
